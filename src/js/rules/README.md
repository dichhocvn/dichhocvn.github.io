# src/js/rules/
- `rules-data.js`: rule text runtime theo cung.
- Dùng bởi `tuvi-render.js`.
- Không sửa thủ công file này nếu đang quản lý rule bằng text.
- Nguồn chuẩn là `src/rules/*.txt`, sau đó chạy:
  - `python3 scripts/sync_rules.py`
